syntax = "proto3";

package grpc1.example;

option java_package = "grpc1.example.proto";
option java_multiple_files = true;

service HelloService {
  rpc StreamHello (StreamRequest) returns (stream HelloMessage);
}

message StreamRequest {}

message HelloMessage {
  string text = 1;
}
